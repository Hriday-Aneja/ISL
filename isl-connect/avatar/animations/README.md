Drop .glb/.fbx sign animation clips here, named after the gloss ID
(e.g. HELLO.glb, THANK_YOU.glb). Kept out of git (see .gitignore) — share
via drive/USB during the hackathon and document where the source of truth lives.
