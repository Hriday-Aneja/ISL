// Wraps the avatar module's viewer component (see /avatar/src/AvatarViewer.jsx).
// Placeholder until that module is wired in.
import React from "react";
export default function AvatarViewport() {
  return (
    <div className="w-full h-full flex items-center justify-center bg-gray-100 rounded-lg">
      <span className="text-gray-400">Avatar viewport</span>
    </div>
  );
}
